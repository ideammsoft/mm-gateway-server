# 2. 여기서부터는 spring: 안으로 들어가면 안 됩니다! (맨 앞으로 당기기)
app:
  jwt:
    secret: ${JWT_SECRET}
    access-token-minutes: 15
    refresh-token-minutes: 20160

my:
  logging:
    level: debug

logging:
  level:
    root: info
    org.springframework.cloud.gateway: ${my.logging.level}
    co.kr.mmsoft.shopgatewayserver: ${my.logging.level}